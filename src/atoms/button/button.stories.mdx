import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import MTVButton from './button.comp';

<Meta title='MDX/Checkbox' component={MTVButton} />

# Button

With `MDX` we can define a story for `MTVButton` right in the middle of our
Markdown documentation.

<!--- This is your Story template function, shown here in React -->

export const Template = (args) => <MTVButton {...args} />;

<Canvas>
  <Story
    name='Default'
    args={{
      color: 'primary',
      label: 'Button'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
